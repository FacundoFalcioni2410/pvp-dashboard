{
  "name": "excel-dashboard",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "setup": "pip install -r ./backend/requirements.txt && npm install --prefix frontend",
    "create-user": "python -m backend.manage_user admin",
    "seed-user": "python -m backend.seed_user",
    "dev": "concurrently --names \"api,web\" --prefix-colors \"cyan,magenta\" \"python -m uvicorn backend.main:app --reload --port 8000\" \"node scripts/wait-for-api.mjs && npm run dev --prefix frontend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
